[%-

CALL site_config.navigation.push(
    {
        text => loc("Conference"),
        url  => '/conference'
    }
);

IF (c.check_user_roles(c.user, 'admin')) ;
    CALL site_config.submenu.push(
        {
            text => loc("Organize a conference"),
            uri  => '/conference/create'
        }
    );
END ;

-%]